## 1. 菜单目标过滤

- [x] 1.1 在 `SongContextMenu` 中为当前歌曲派生可添加分组列表，排除系统分组“我的收藏”。
- [x] 1.2 从可添加分组列表中排除当前正在浏览的分组。
- [x] 1.3 从可添加分组列表中排除已经包含当前歌曲的普通用户分组。
- [x] 1.4 仅当可添加分组列表非空时显示“添加到分组”子菜单，并避免留下多余分隔线。
- [x] 1.5 保留 `AppState.addSong(_:to:)` 的重复添加保护和 toast 行为，不改变持久化结构。

## 2. OpenSpec 同步

- [x] 2.1 将 `music-library` delta spec 同步到主规格，更新歌曲右键菜单要求。
- [x] 2.2 将 `music-groups` delta spec 同步到主规格，更新添加歌曲到分组的目标范围。
- [x] 2.3 归档完成后的 OpenSpec change，并确保主规格保留新的菜单行为。

## 3. 验证

- [x] 3.1 运行 `openspec validate refine-add-to-group-menu`。
- [x] 3.2 运行 `swift build`。
- [x] 3.3 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`。
- [x] 3.4 手动验证资料库视图中“添加到分组”只显示可添加普通分组，不显示“我的收藏”或已包含该歌曲的分组。
- [x] 3.5 手动验证普通分组视图中右键歌曲时，“添加到分组”不显示当前分组，仍可显示其他可添加普通分组。
- [x] 3.6 手动验证“我的收藏”视图中右键歌曲时，“添加到分组”不显示“我的收藏”，但仍可显示其他可添加普通分组。
- [x] 3.7 手动验证无任何可添加目标时，右键菜单不显示“添加到分组”入口，且“从此分组移除”或“从资料库中删除”仍按当前视图显示。
