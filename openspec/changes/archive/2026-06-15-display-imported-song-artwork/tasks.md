## 1. 数据模型与持久化

- [x] 1.1 为 `Album` 增加可选封面数据字段，并保持旧曲库 JSON 缺少该字段时可正常解码
- [x] 1.2 为 `Song` 增加可选歌曲级封面数据字段，用于无专辑名但含内嵌封面的导入歌曲
- [x] 1.3 确认曲库持久化会保存新增封面字段，且清空歌曲列表时封面数据随歌曲和专辑数据一起清理

## 2. 导入元数据解析

- [x] 2.1 在 `parseMetadata(url:)` 中读取 `.commonKeyArtwork`，提取可用图片数据并忽略空数据或不可解析数据
- [x] 2.2 导入含专辑名歌曲时，将可用封面写入新建专辑，或在已有专辑缺少封面时补写
- [x] 2.3 导入无专辑名但含封面的歌曲时，将封面保存为歌曲级封面
- [x] 2.4 确保封面缺失或解析失败不会阻断歌曲导入，文字元数据与时长仍按现有规则回退
- [x] 2.5 音频文件无内嵌封面时，读取同目录同名图片文件作为歌曲级封面
- [x] 2.6 启动时为已导入但缺封面的历史歌曲回填同名图片封面

## 3. 封面渲染

- [x] 3.1 扩展 `CoverView`，支持优先渲染歌曲级封面，其次渲染专辑封面，最后回退现有渐变占位符
- [x] 3.2 更新歌曲表格标题列调用，使导入封面显示为缩略图，缺失或不可渲染时显示统一占位符
- [x] 3.3 更新播放条和队列面板的封面调用，保持与歌曲表格一致的封面/占位符逻辑
- [x] 3.4 保持现有占位符视觉风格、尺寸、圆角和边框行为不变

## 4. 验证

- [x] 4.1 运行 `swift build` 验证 SwiftPM 编译通过
- [x] 4.2 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`
- [x] 4.3 手动验证导入含内嵌封面的音频文件后，歌曲表格显示该封面
- [x] 4.4 手动验证导入无封面或封面不可解析的音频文件后，歌曲表格显示占位符且导入成功
- [x] 4.5 手动验证重启应用后，已导入歌曲的封面或占位符显示状态保持一致
