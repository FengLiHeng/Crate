## 1. 播放会话重置

- [x] 1.1 在 `PlayerStore` 新增曲库级播放会话重置方法，停止播放并清空插播队列、播放上下文和当前曲目状态

## 2. 曲库清空逻辑

- [x] 2.1 在 `AppState` 新增一键清空歌曲列表方法，清空歌曲、专辑、失效标记和分组内歌曲引用
- [x] 2.2 清空后回到“歌曲”视图，清除选中项，并显示清空完成 toast

## 3. 用户界面

- [x] 3.1 在歌曲列表头部添加清空入口，仅在资料库非空时启用
- [x] 3.2 为清空入口添加确认弹窗，取消时不改变资料库、分组或播放状态

## 4. 验证

- [x] 4.1 运行 `openspec validate clear-music-library`
- [x] 4.2 运行 `swift build`
- [x] 4.3 运行 `Scripts/bundle.sh`
