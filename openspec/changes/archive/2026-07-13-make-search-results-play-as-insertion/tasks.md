## 1. 搜索插播状态机

- [x] 1.1 在 `PlayerStore` 新增搜索结果插播操作：保留已有插播与上下文顺序、移除目标歌曲的后续重复项，并在无上下文时建立不轮转的后续上下文。
- [x] 1.2 为搜索插播的既有上下文保序去重、首次搜索播放原始顺序、普通 `playFrom` 轮转和 A→B 上下文替换补充 `PlayerStoreTests`。

## 2. 歌曲表格交互

- [x] 2.1 让歌曲表格在搜索词非空时调用搜索插播操作；无搜索词时继续使用现有上下文重建逻辑。

## 3. 验证

- [x] 3.1 运行 OpenSpec 校验、Swift 测试与构建，确认搜索插播和既有播放行为均通过回归验证。
- [x] 3.2 运行打包脚本，更新 `build/Crate.app` 供手动验证搜索与分组场景。
