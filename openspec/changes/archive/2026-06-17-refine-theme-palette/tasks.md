## 1. 主题令牌

- [x] 1.1 将浅色主题调整为暖白基底与酒红强调色。
- [x] 1.2 将深色主题调整为石墨黑基底与铜色强调色。
- [x] 1.3 收紧封面占位、播放条氛围、控制区、分隔线、悬停和选中令牌。

## 2. 组件收敛

- [x] 2.1 将局部硬编码的滑块描边和错误提示色收敛到主题语义。

## 3. 验证

- [x] 3.1 运行 `openspec validate refine-theme-palette` 验证变更通过。
- [x] 3.2 运行 `swift build` 验证编译通过。
- [x] 3.3 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`。
