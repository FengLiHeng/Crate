## ADDED Requirements

### Requirement: 批量添加与拖放歌曲到普通分组
应用 SHALL 允许用户将当前选择的多首歌曲批量添加到普通用户分组，也 SHALL 允许把所选歌曲从歌曲表格拖放到侧边栏普通分组。目标列表和拖放目标 MUST 排除系统收藏分组；目标分组中已存在的歌曲 SHALL 保持单一引用，新歌曲 SHALL 按当前可见选择顺序追加。

#### Scenario: 通过批量菜单添加到分组
- **WHEN** 用户选择多首歌曲并从批量操作中选择一个普通分组
- **THEN** 尚未存在于目标分组的歌曲按当前可见顺序追加，已有歌曲不重复

#### Scenario: 拖放选择到普通分组
- **WHEN** 用户从歌曲表格拖动当前选择并放到侧边栏普通分组
- **THEN** 有效歌曲按当前可见顺序添加到目标分组并显示操作反馈

#### Scenario: 系统收藏不接受歌曲拖放
- **WHEN** 用户把歌曲选择拖动到“我的收藏”
- **THEN** 侧边栏不接受该拖放，收藏内容保持不变

#### Scenario: 拖放去重
- **WHEN** 拖入的选择包含目标分组已有歌曲
- **THEN** 已有歌曲不重复，新歌曲仍正常追加
