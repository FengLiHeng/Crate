## 1. 视觉令牌

- [x] 1.1 调整浅色/深色主题的强调色与表面色，使界面从暖橙渐变风格转为克制的唱片箱风格。
- [x] 1.2 增加封面占位符所需的纸套、唱片、槽纹、边框和标签色令牌。

## 2. 封面组件

- [x] 2.1 将 `CoverView` 的缺省渲染改为统一唱片/纸套式占位符。
- [x] 2.2 让分组马赛克封面复用同一占位符，并在有真实专辑封面时优先显示真实图像。
- [x] 2.3 统一真实封面与占位封面的边框、圆角、阴影和高光处理。

## 3. 播放条

- [x] 3.1 将播放条专辑氛围背景改为低透明度、低饱和度色相反馈。
- [x] 3.2 将未播放状态的封面占位态与歌曲列表中的占位符保持一致。

## 4. 验证

- [x] 4.1 运行 `swift build` 验证编译通过。
- [x] 4.2 运行 `openspec validate refine-cover-placeholder-ui` 验证规格变更通过。
- [x] 4.3 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`。
