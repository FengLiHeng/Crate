## 1. LRC 解析与测试基础

- [x] 1.1 新增 `LyricLine`、`ParsedLyrics` 和 `LRCParser` 等纯 Foundation 类型，支持标准时间戳、同一行多个时间戳、元信息跳过和按时间排序输出。
- [x] 1.2 实现 `[offset:+/-毫秒]` 偏移处理，并保证偏移后时间不小于 0。
- [x] 1.3 增加歌词文件字符串解码辅助，按 UTF-8、UTF-16、UTF-16LE、UTF-16BE 和系统默认解码顺序读取 `.lrc` 数据。
- [x] 1.4 新增 SwiftPM 单元测试覆盖多时间戳、offset、无效行、空歌词、排序和解析失败场景。

## 2. 歌词状态与文件发现

- [x] 2.1 在 `AppState` 增加歌词页状态模型，保存当前歌词对应歌曲 id、歌词文件 URL 和解析结果。
- [x] 2.2 在 `AppState` 增加从当前歌曲真实文件路径推导同名 `.lrc` 路径的方法，点击时按当前磁盘状态懒检测。
- [x] 2.3 实现打开歌词页流程：无当前歌曲、无真实文件路径、缺少同名 `.lrc`、读取失败或解析失败时显示对应 toast 并保持当前界面。
- [x] 2.4 实现关闭歌词页流程，关闭时不改变播放状态、队列状态或当前资料库视图。
- [x] 2.5 处理歌词页打开期间当前歌曲变化：新歌曲有可解析同名歌词时刷新页面，无歌词或解析失败时关闭页面并 toast。

## 3. 播放条入口

- [x] 3.1 将 `PlayBarView` 左侧当前歌曲封面改为可点击歌词入口，未播放占位态保持原视觉和禁用语义。
- [x] 3.2 为封面入口增加 hover/按压反馈和 help 文案，使用户能发现“打开歌词页”交互。
- [x] 3.3 确保封面入口失败时复用 `AppState` toast，不影响播放/暂停、进度、音量和队列按钮。

## 4. 歌词播放动态页面

- [x] 4.1 新增 `LyricsPlaybackView`，显示当前歌曲封面、标题、艺人、歌词列表和返回/关闭入口，底部播放条保持可见可操作。
- [x] 4.2 在 `RootView` 主内容区域承载歌词页覆盖层或替换层，关闭后回到进入前的主界面。
- [x] 4.3 根据 `PlayerStore.progress` 二分定位当前歌词行，并仅在当前行变化时自动滚动到该行附近。
- [x] 4.4 为当前歌词行、前后歌词行和空白/间奏状态设计清晰的视觉层级，浅色和深色主题均保持可读。
- [x] 4.5 支持点击带时间戳的歌词行调用 `PlayerStore.seek(to:)` 跳转播放位置。
- [x] 4.6 验证播放暂停、拖动底部进度条、上一首、下一首和自动切歌后歌词页内容与高亮状态同步。

## 5. 验证与收尾

- [x] 5.1 运行 `swift test`，确认 LRC 解析器测试通过。
- [x] 5.2 运行 `swift build`，确认应用编译通过。
- [x] 5.3 手动验证有同名 `.lrc`、无 `.lrc`、解析失败 `.lrc`、导入后补放 `.lrc`、歌词行点击 seek 和切歌刷新/关闭行为。
- [x] 5.4 运行 `Scripts/bundle.sh`，确保最新可测试 app bundle 生成到 `build/Crate.app`。
- [x] 5.5 更新本 change 的 `tasks.md` 勾选完成项，并在实现完成后执行 `openspec validate add-local-lrc-lyrics`。
