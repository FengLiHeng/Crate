## 1. 曲库默认数据

- [x] 1.1 修改 `AppState.init()` 的无持久化数据分支，默认使用空 `albums`、`library` 和 `playlists`
- [x] 1.2 确认运行时不再引用 `SampleData` 作为默认曲库来源
- [x] 1.3 检查空资料库状态在首次启动时显示 0 首歌曲且不显示虚拟播放列表

## 2. 导入入口与目录扫描

- [x] 2.1 保留现有 `importViaPanel()` 文件多选导入行为
- [x] 2.2 新增 `importFolderViaPanel()`，打开仅可选择单个目录的系统面板
- [x] 2.3 为目录导入读取所选目录的直接子项，并过滤普通文件 URL
- [x] 2.4 将目录中筛选出的文件 URL 交给 `importFiles(_:)` 处理，复用扩展名过滤、去重、解码校验、元数据解析、toast 和持久化逻辑
- [x] 2.5 确认目录中无支持格式音频文件时提示"未发现可导入的音频文件"

## 3. 头部 + 菜单 UI

- [x] 3.1 将 `LibraryContentView` 内容区头部的单一 `+` 按钮改为视觉尺寸一致的 SwiftUI `Menu`
- [x] 3.2 在菜单中添加"导入文件"动作并连接到现有文件导入入口
- [x] 3.3 在菜单中添加"导入文件夹"动作并连接到新的目录导入入口
- [x] 3.4 确认搜索、清理失效曲目、主题切换等相邻工具按钮布局不发生异常位移

## 4. 验证

- [x] 4.1 运行可用的 Swift 构建或测试命令，确认项目可编译
- [x] 4.2 手动验证首次启动无持久化曲库时为空资料库和空歌单列表
- [x] 4.3 手动验证通过 + 菜单导入多个音频文件后歌曲加入资料库并显示导入数量
- [x] 4.4 手动验证通过 + 菜单导入目录时仅导入直接子项中的支持格式音频文件
- [x] 4.5 手动验证重复路径、不可解码文件和无有效音频文件的提示仍符合现有导入行为
