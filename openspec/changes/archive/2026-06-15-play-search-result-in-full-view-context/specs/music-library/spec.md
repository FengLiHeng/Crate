## MODIFIED Requirements

### Requirement: 搜索过滤
内容区头部 SHALL 提供搜索框，按标题、艺术家、专辑名进行不区分大小写的包含匹配，实时过滤当前视图的歌曲；有输入时 SHALL 显示清除按钮。搜索过滤 SHALL 只影响表格可见歌曲和统计显示，MUST NOT 缩小从当前视图发起播放时用于重建待播清单的歌曲集合。

#### Scenario: 搜索命中
- **WHEN** 用户在搜索框输入关键字
- **THEN** 表格仅显示标题/艺术家/专辑包含该关键字的歌曲，统计数字同步更新

#### Scenario: 搜索无结果
- **WHEN** 搜索词没有任何匹配
- **THEN** 显示"没有与「关键字」匹配的结果"提示

#### Scenario: 搜索结果播放仍使用完整分组
- **WHEN** 用户在某分组内搜索到一首歌曲并双击播放
- **THEN** 该歌曲立即播放，待播清单按该分组完整歌曲集合重建，而不是只包含搜索结果

### Requirement: 视图播放入口
内容区头部 SHALL 提供"播放"与"随机播放"按钮：播放 SHALL 先清空旧待播清单中的插播队列，再从当前视图（不含搜索过滤）第一首顺序开始，并将当前视图歌曲作为新的后续待播上下文；随机播放 SHALL 先清空旧插播队列，再开启随机模式并以当前视图（不含搜索过滤）歌曲生成随机待播上下文。当前视图无歌曲时两按钮 SHALL 禁用。

#### Scenario: 点击播放
- **WHEN** 待播清单中已有旧插播曲目，用户在当前视图点击“播放”
- **THEN** 旧插播队列被清空，从当前视图第一首开始播放，并以当前视图歌曲重建后续待播

#### Scenario: 搜索后点击播放
- **WHEN** 用户在某分组内输入搜索词后点击“播放”
- **THEN** 旧插播队列被清空，从该分组完整歌曲集合的第一首开始播放

#### Scenario: 点击随机播放
- **WHEN** 用户在某分组视图点击“随机播放”
- **THEN** 随机模式开启，旧插播队列被清空，并从该分组完整歌曲集合随机一首开始播放
