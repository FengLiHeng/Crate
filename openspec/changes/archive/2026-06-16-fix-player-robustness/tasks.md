## 1. 播放状态机

- [x] 1.1 修复 `PlayerStore` 的异步播放代次废弃逻辑，确保停止、删除和清空后旧引擎不会重新播放。
- [x] 1.2 修复全量不可播、插播结束回归和立即播放期间点下一首的状态推进。
- [x] 1.3 补充播放状态机单元测试，覆盖不可播遍历、异步停止竞态和立即播放回归。

## 2. 曲库持久化

- [x] 2.1 增加曲库读取失败保护，启动阶段不得自动覆盖损坏或不可解码的 `library.json`。
- [x] 2.2 暴露持久化写入失败提示，并保持终止前强制 flush 行为。
- [x] 2.3 补充持久化保护单元测试。

## 3. 导入与封面

- [x] 3.1 将导入解码、元数据解析和 sidecar 封面读取移到后台，主 actor 批量提交导入结果。
- [x] 3.2 限制持久化封面数据尺寸，必要时压缩缩略图，失败时跳过封面但继续导入。
- [x] 3.3 移除 OGG 作为承诺支持格式，并同步导入过滤和 UI 文案。

## 4. 失效曲目

- [x] 4.1 合并启动失效探测结果时保留探测期间的用户修复和即时失效标记。
- [x] 4.2 重新定位时校验所选文件可解码，批量修复时仅修复可解码候选文件。

## 5. 验证与收尾

- [x] 5.1 运行 `openspec validate fix-player-robustness`。
- [x] 5.2 运行 `swift build` 和新增测试。
- [x] 5.3 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`。
