## 1. 队列状态反馈

- [x] 1.1 在 `PlayerStore` 中记录并安全清除最近设为下一首的歌曲 id
- [x] 1.2 在现有“下一首播放”成功路径中触发置顶反馈，同时保留 toast 和播放记忆行为

## 2. 待播面板

- [x] 2.1 将手动队列与上下文后续曲目连续渲染在单一“接下来”分区
- [x] 2.2 保留两类队列行各自的播放、移除、拖动排序及无障碍动作
- [x] 2.3 为最近置顶的第一行添加兼容浅色、深色与减少动态效果的短暂强调

## 3. 验证与规格同步

- [x] 3.1 校验 OpenSpec change 并检查源码中不再渲染“插播 · 下一首播放”分区标题
- [x] 3.2 运行 `swift build` 验证编译
- [x] 3.3 将 delta spec 同步到主规格并运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`
