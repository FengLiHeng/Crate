## 1. 数据与分组规则

- [x] 1.1 在 `AppState` 中定义“我的收藏”的固定 ID、名称和系统分组判断 helper。
- [x] 1.2 在启动加载持久化数据后规范化 `playlists`，缺失时自动插入“我的收藏”，存在时强制名称正确并移动到第一位。
- [x] 1.3 更新分组创建、重命名、删除和排序逻辑，确保“我的收藏”不可重命名、不可删除、不可拖动，普通分组只能排在其后。
- [x] 1.4 更新删除歌曲、清空曲库、清理失效曲目等分组内容同步逻辑，保留“我的收藏”容器并正确移除失效歌曲 ID。

## 2. 收藏行为

- [x] 2.1 在 `AppState` 中新增 `favoritesGroup`、`isFavorite(_:)` 和 `toggleFavorite(_:)` 行为。
- [x] 2.2 实现收藏当前歌曲时追加到“我的收藏”末尾，并避免重复歌曲 ID。
- [x] 2.3 实现取消收藏当前歌曲时仅从“我的收藏”移除该歌曲 ID，不影响资料库、待播清单和播放上下文。
- [x] 2.4 为收藏和取消收藏显示简体中文 toast 反馈。

## 3. 界面集成

- [x] 3.1 更新侧边栏分组项上下文菜单，使“我的收藏”不显示重命名和删除入口。
- [x] 3.2 更新侧边栏拖动交互，使“我的收藏”不能被拖动，普通分组拖动目标不会越过第一位。
- [x] 3.3 在底部播放条播放控制按钮行增加星型或爱心型收藏按钮，并根据 `isFavorite(_:)` 展示普通/选中图标状态。
- [x] 3.4 确保无当前歌曲时收藏按钮不可执行或不展示，播放条布局在长标题下不出现挤压错位。
- [x] 3.5 更新歌曲右键菜单，资料库视图显示“从资料库中删除”，分组视图显示“从此分组移除”。

## 4. 验证

- [x] 4.1 运行 `swift build` 验证编译通过。
- [x] 4.2 手动验证首次启动和历史数据启动都会出现第一位“我的收藏”分组。
- [x] 4.3 手动验证“我的收藏”不可重命名、不可删除、不可拖出第一位，普通分组仍可创建、重命名、删除和排序。
- [x] 4.4 手动验证播放条收藏/取消收藏当前歌曲后，“我的收藏”列表和按钮状态同步，且播放状态、进度和待播清单不变。
- [x] 4.5 运行 `Scripts/bundle.sh` 生成最新 `build/Crate.app`。
- [x] 4.6 手动验证分组视图移除歌曲只影响当前分组，资料库视图仍可从资料库删除歌曲。
