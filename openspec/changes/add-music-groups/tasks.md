## 1. 状态与数据操作

- [x] 1.1 在 `AppState` 中新增创建分组的方法：裁剪名称空白、拒绝空名称、创建唯一 id 的空 `Playlist`、持久化并切换到新分组视图
- [x] 1.2 调整添加歌曲到分组的用户可见 toast 文案，从“播放列表/歌单”统一为“分组”
- [x] 1.3 确认删除歌曲、清理失效曲目和重启持久化仍同步处理所有分组中的歌曲 id
- [x] 1.4 增加统一分组名称校验：1 到 24 个字符、折叠空白、禁止非法字符/纯符号/同名
- [x] 1.5 增加重命名分组、删除分组、拖动排序分组的 `AppState` 方法，并保持持久化

## 2. 侧边栏分组入口

- [x] 2.1 将侧边栏标题“播放列表”改为“分组”，现有导航项继续展示持久化分组
- [x] 2.2 在“分组”标题行右侧添加 `+` 图标按钮，并提供“新建分组”帮助文本
- [x] 2.3 实现新建分组命名界面，确认有效名称后调用 `AppState` 创建方法，取消时不改变数据
- [x] 2.4 创建成功后侧边栏显示新分组，内容区切换到该分组视图并显示选中态
- [x] 2.5 在分组项右键菜单中提供“重命名”和“删除分组”
- [x] 2.6 复用命名界面实现分组重命名，并展示名称校验错误
- [x] 2.7 删除分组前弹出确认，确认后删除分组；当前视图为被删分组时切回“歌曲”
- [x] 2.8 支持拖动分组项调整排序并持久化

## 3. 歌曲列表与右键菜单

- [x] 3.1 将歌曲右键菜单“添加到播放列表”改为“添加到分组”
- [x] 3.2 在“添加到分组”子菜单中列出现有分组，选择后将歌曲追加到目标分组且避免重复
- [x] 3.3 当前没有分组时，在子菜单中显示不可用的“暂无分组”空状态项
- [x] 3.4 为分组空视图提供专用空状态文案，引导用户从歌曲右键菜单添加歌曲

## 4. 文案与规格一致性

- [x] 4.1 检查用户可见文案，确保左侧持久化集合统一称为“分组”
- [x] 4.2 保留右下角“待播清单”文案与行为不变，确认不会因添加到分组改变队列或播放上下文
- [x] 4.3 检查内容区标题、统计、播放和随机播放在分组视图中继续可用

## 5. 验证

- [x] 5.1 运行 `swift build`
- [x] 5.2 手动验证首次启动无持久化数据时侧边栏不显示虚拟分组
- [x] 5.3 手动验证创建分组、空名称不可创建、重启后分组保留
- [x] 5.4 手动验证歌曲可添加到分组、重复添加不产生重复歌曲、删除歌曲会从分组移除
- [x] 5.5 手动验证“添加到分组”不改变待播清单、当前播放上下文和播放控制
- [x] 5.6 运行 `openspec validate add-music-groups`
- [ ] 5.7 手动验证分组名称长度、非法字符、纯符号、同名校验
- [ ] 5.8 手动验证分组重命名、删除和拖动排序
